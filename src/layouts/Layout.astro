<!DOCTYPE html>
<html lang="en">
  <head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <meta name="generator" content={Astro.generator} />
  <meta name="description" content="StackSmart AI: Tools and strategies for solopreneurs using AI to scale smart." />
  <title>StackSmart AI</title>
</head>
    <style>
      :root {
        --font-sans: system-ui, sans-serif;
        --bg: #1a1a1a;
        --fg: #f8f8f8;
        --accent: #00ffff;
      }

      body {
        background: var(--bg);
        color: var(--fg);
        font-family: var(--font-sans);
        margin: 0;
        padding: 2rem;
      }

      a {
        color: var(--accent);
        text-decoration: none;
        font-weight: bold;
      }

      h1 {
        color: var(--accent);
      }

      p {
        font-size: 1.1rem;
        line-height: 1.6;
      }
    </style>
  </head>
  <body>
    <slot />
  </body>
</html>
